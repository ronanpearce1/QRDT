<style>
    body {
        margin: 1rem;
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 20px;
        background-image: linear-gradient(to bottom right, rgb(0, 215, 253), rgb(193, 10, 248));
        background-repeat: no-repeat;
        background-attachment: fixed;
        animation: fadeInAnimation ease 2s;
        animation-iteration-count: 1;
        animation-fill-mode: forwards;
    }



    .container {
        width: 400px;
        max-width: 400px;
        padding-top: min(100px);
        margin: auto;
        padding: 2rem;
        box-shadow: 0 0 40px rgba(0, 0, 0, 0.5);
        border-radius: 50px;
        background-color: white;
    }

    @keyframes fadeInAnimation {
        0% {
            opacity: 0%;
        }

        100% {
            opacity1: 100%;
        }
    }

    .img {
        display: block;
        margin-left: auto;
        margin-right: auto;
    }

    p {
        text-align: justify;
        font-size: smaller;
    }

    .container,
    .entry,
    .button,
    .logoutBtn,
    button {
        font-family: 'Barlow', sans-serif;

    }



    .form>*:first-child {
        margin-top: 0;
    }

    .form>*:last-child {
        margin-top: 0;
    }

    .error {
        color: red;
        text-align: center;
    }

    textarea {
        resize: none;
    }

    table {
        border-collapse: collapse;
        width: 100%;
        table-layout: fixed
    }

    td,
    th {
        border: 1px solid #dddddd;
        text-align: left;
        padding: 8px;
        overflow: hidden;
        overflow: auto;
    }

    tr:nth-child(even) {
        background-color: #dddddd;
    }

    .entry,
    #users {
        margin-bottom: 1rem;
        display: block;
        width: 100%;
        padding: 0.75rem;
        box-sizing: border-box;
        border-radius: 20px;
        border: 1px solid #dddddd;
        outline: none;
        background: #eeeeee;
        transition: background 0.2s, border-color 0.2s;
        font-size: 15px;
        text-align: center;
        transition: 0.4s;
    }

    .entry:hover {
        background-color: #cfcfcf;
        transition: 0.4s;
        font-size: 20px;
        cursor: pointer;
    }

    .changeForm {
        text-align: center;
        color: aliceblue;
    }

    button,
    .button,
    label {
        margin-bottom: 1rem;
        display: block;
        width: 100%;
        padding: 0.75rem;
        box-sizing: border-box;
        border-radius: 20px;
        border: 1px solid #dddddd;
        outline: none;
        background: #d6d6d6;
        transition: background 0.2s, border-color 0.2s;
        font-size: 15px;
        text-align: center;
        transition: 0.4s;
    }

    button:hover,
    .button:hover,
    label:hover {
        background-color: rgb(52, 160, 252);
        color: white;
        transition: 0.4s;
        font-size: 20px;
        cursor: pointer;
    }

    .logoutBtn:hover {
        background-color: rgb(193, 10, 248);
    }

    .changeForm {
        text-decoration: none;
        color: black;
        display: block;
        transition: 0.4s;
    }

    .changeForm:hover {
        text-decoration: underline;
        font-size: 22px;
        transition: 0.4s;
    }

    #adminButton,
    #userButton:hover {
        cursor: pointer;

    }

    #videoElement {
        width: 100%;
        display: block;
        background-color: white;
        border-radius: 10px;
        box-shadow: inset 0 0 40px rgba(0, 0, 0, 0.9);
    }

    .select,
    .capture {
        visibility: hidden;
        width: 0px;
        height: 0px;
    }

    #qr {
        margin: auto;
        display: flex;
        align-items: center;
        justify-content: center;
        width: max-content;
        padding: 1rem;
        border: 5px solid;
        border-image: linear-gradient(rgb(0, 215, 253), rgb(193, 10, 248)) 30;
    }


    #qrcode {
        margin: auto;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
    }


    .generate,
    .resetQRBtn {
        display: none;
        margin: auto;
        width: 19rem;
    }



    @media only screen and (min-width: 500px) {
        #labelForCapture {
            display: none;
        }
    }


    .material-symbols-outlined {
        font-variation-settings:
            'FILL' 0,
            'wght' 400,
            'GRAD' 0,
            'opsz' 48;

    }

    .material-symbols-outlined:hover {
        cursor: pointer;
    }

    .grid {
        display: flex;
        width: 100%;

    }

    .grid span {
        flex: 1;
        display: flex;
        justify-content: flex-start;
        align-items: center;
    }

    .grid div {
        flex: 1;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .grid p {
        flex: 1;
        display: flex;
        justify-content: flex-end;
        align-items: center;
    }



    .formTitle {
        margin-bottom: 2rem;
        text-align: center;
    }
</style>